{
  "scripts": {
    "compile:nix": "rm -rf modules && tsc",
    "compile:win": "rmdir /s /q modules && tsc",
    "pack:nix": "npm run compile:nix && cp src/.reciple modules && cd src && rmm pack ./module.zip",
    "pack:win": "npm run compile:win && copy src\\.reciple modules && cd src && rmm pack ./module.zip",
    "test:nix": "npm run compile:nix && npm run start",
    "test:win": "npm run compile:win && npm run start",
    "start": "reciple"
  },
  "dependencies": {
    "minecraft-protocol": "^1.34.0",
    "reciple": "^1.1.4"
  },
  "devDependencies": {
    "@types/node": "^17.0.34",
    "reciple-module-manager": "^1.1.2",
    "typescript": "^4.6.4"
  }
}
