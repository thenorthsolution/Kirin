{
    "name": "kirin",
    "private": true,
    "license": "GPL-3.0",
    "scripts": {
        "clean": "npx rimraf modules",
        "build": "npm run clean && npx tsc",
        "pack": "npm run build && cp ./src/.reciple.yml ./modules && npx rmm pack --module-dir ./modules --remove-old-file true",
        "start": "npx reciple -y",
        "test": "npm run build && npx reciple -D -y"
    },
    "dependencies": {
        "discord.js": "^14.7.1",
        "fallout-utility": "^2.1.3",
        "minecraft-protocol": "^1.40.3",
        "reciple": "^7.0.9"
    },
    "devDependencies": {
        "@falloutstudios/rmm": "^3.3.4",
        "@types/node": "^17.0.45",
        "rimraf": "^3.0.2",
        "typescript": "^4.9.5",
        "yaml": "^2.2.1"
    }
}
